global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'streaming-server'
    static_configs:
      - targets: ['streaming-server:9090']
        labels:
          service: 'streaming-server'

  - job_name: 'ml-optimizer'
    static_configs:
      - targets: ['ml-optimizer:8081']
        labels:
          service: 'ml-optimizer'